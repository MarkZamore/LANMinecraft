# Что нового

## 331
- Сборка, у которой пропали файлы загрузчика, чинится сама: раньше она падала при запуске и оставалась так навсегда. Все сборки один раз подготовятся заново.

## 330
- При открытии мира для сети в лог больше не падает ошибка на старых версиях игры: строчку в чат там написать нечем, и это не повод для исключения. Мир открывался и раньше.

## 329
- Уборка общего хранилища больше не трогает файлы загрузчика: она собиралась удалить то, чем игра запускается, и сборки перестали бы стартовать. Обновитесь до запуска лаунчера.

## 328
- При запуске лаунчер проверяет свои папки и убирает то, чем не пользуется ни одна сборка: подготовку удалённых сборок и копии игры, оставшиеся с тех пор, когда каждая качала её себе. Миры не трогаются никогда.

## 327
- Большая куча больше не даёт долгих пауз: молодое поколение теперь измеряется в гигабайтах, а не в долях кучи. Раньше 16 ГБ означали сборку по шесть гигабайт за раз и паузу в 202 мс вместо обещанных 50.

## 326
- В All The Fabric 3 вернулся выбор языка: уборка нового общего хранилища удаляла указатель ассетов, потому что его никто не числил нужным. Сборки один раз подготовятся заново.

## 325
- Вернулись скины, убранное окно при открытии для сети и дозирование чанков: прошлое обновление перенесло библиотеки игры, а искали их по-старому. Из-за этого у гостя снова были задержки.

## 324
- Сборка RPG Ars Nouveau снята и больше не предлагается в списке. У тех, кто её уже скачал, она остаётся и запускается, но обновлять и скачивать заново её неоткуда.

## 323
- Игра, библиотеки и ресурсы качаются один раз на все сборки, а не заново для каждой: новая сборка на уже знакомой версии займёт около 150 МБ вместо гигабайта с лишним. Что не нужно ни одной сборке, удаляется само.

## 322
- Java, которую сборки ставили себе поодиночке до того, как она стала общей, теперь убирается при запуске: на диске освобождается по 300 МБ за каждую такую сборку. Java от Mojang рядом с ней не трогается: она нужна установщику загрузчика.

## 321
- Диалоги лаунчера больше не крупнее его самого: они берут масштаб у окна, а вопрос про пресет управления перестал быть системным окном. Сборка, которую ещё не скачали, теперь взвешивается по своему манифесту, а не по размеру машины.

## 320
- Рядом с пресетом управления появилась корзина: она удаляет выбранную сборку с компьютера со всем, что лаунчер для неё готовил. Миры удаляются только отдельным выбором, а Java уходит, если её больше не просит ни одна сборка.

## 319
- В список сборок добавлена Create & Ars: Arcane Awakened, 88 модов на 1.21.1, собранных её автором под слабую машину. Прорисовка и симуляция в ней уже выставлены так, чтобы её тянул хост.

## 318
- Каждая пауза сборщика мусора попадает в лог рядом с отчётом сервера. Набору флагов для большой кучи вернули то, чего в нём не хватало.

## 317
- Гостя больше не выбрасывает с «Соединение потеряно»: чанки шли быстрее, чем держит канал. Теперь лаунчер сбавляет темп, когда ответы начинают запаздывать.

## 316
- В лог попадает круговая задержка до каждого гостя. Это последнее число, которого не хватало, чтобы объяснить его задержки.

## 315
- В списке для отправки отчёта теперь только имя игрока, без статуса. Отчёт возьмёт кто угодно, так что выбору он не помогал.

## 314
- Игрок, закрывший лаунчер прямо в игре, остаётся в списке со своей сборкой и версией. Раньше он просто пропадал.

## 313
- В списке игроков видно номер чужого лаунчера. Списки передачи мира больше не блокируются во время игры.

## 312
- Обновление сборки больше не теряется, если файл поменялся без изменения размера. Из-за этого четыре раза не доезжала правка задержки телепорта.

## 311
- Пояснение к дозированию чанков приведено в соответствие с замерами. Для игрока ничего не меняется.

## 310
- В лог раз в минуту попадает, сколько миллисекунд занимает тик сервера. Игра сообщает об этом только при отставании на две секунды.

## 309
- В списке игроков видно, где кто: в лаунчере, в какой сборке или в другой игре. Игроки без лаунчера теперь тоже в списке.

## 308
- Проверка перед выпуском теперь гоняется по настоящим файлам игры. Для игрока ничего не меняется.

## 307
- В лог попадает, на какую дальность раздаётся мир и как часто она меняется. Это нужно, чтобы понять, почему у гостя дёргаются сущности.

## 306
- Гостя больше не откидывает назад при провале связи. Игра считала такой рывок читом и возвращала его на место.

## 305
- Описания в файлах разработки приведены в соответствие с кодом. Для игрока ничего не меняется.

## 304
- Лаунчер записывает в лог, сколько чанков он придержал для каждого гостя. Это нужно, чтобы понять, почему у гостя всё ещё бывают рывки.

## 303
- На версиях до 1.20.2 хост отдаёт гостю чанки порциями, а не залпом. Раньше из-за этого у гостя еда не съедалась, удары доходили с задержкой и его отбрасывало назад.

## 302
- Мир без метки сборки остаётся в корне папки Worlds, а не в отдельной общей папке.

## 301
- Мир от друга сразу попадает в папку своей сборки. Если сборка в нём не указана, он ложится в общую папку и ни одна сборка его не откроет.

## 300
- Миры лежат в Worlds по папкам сборок, и на RPG Ars Nouveau снова открываются. Пустые папки-заглушки больше не нужны и убираются.

## 299
- Текст, который печатает игра, больше не превращается в мусор в отчётах. Лаунчер читал его в системной кодировке, а игра пишет в UTF-8.

## 298
- Уменьшение дальности прорисовки больше не вызывает задержек: лаунчер зря пересылал всё кольцо чанков, которые у игрока уже есть.

## 297
- В отчёт о соединении с другом теперь пишется поток, очередь и качество канала. По одному лишь пингу нельзя было понять, почему гостя выбрасывает.

## 296
- В настройках игры прорисовка, симуляция и дальность теней теперь не выше 16 чанков. Раньше там стояло 32, а мир всё равно показывался на 16.

## 295
- Мир раздаётся не дальше 16 чанков на всех сборках. На 32 гость не успевал получить вид до разрыва по таймауту, а хост зря генерировал землю.

## 294
- Хост с малой прорисовкой больше не оказывается в пустых чанках, когда гость играет с большой. Лаунчер отсеивал чанки слишком близко к краю.

## 293
- Убрана причина пустых чанков и прыгающих существ: при сбое чтения лаунчер отдавал игре отрицательную дальность. Теперь берётся последняя удачная.

## 292
- У хоста больше не остаются дыры в новых чанках, когда гость играет с большой прорисовкой. Раньше они пропадали только после смены дальности.

## 291
- All The Fabric 3 и RPG Ars Nouveau больше не готовятся заново при каждом запуске: лаунчер сверял их Java не с той, что им нужна. Запускаются быстрее и без связи с Mojang.

## 290
- Сборка The Broken Script Enhanced больше не предлагается к скачиванию. У кого папка с ней уже есть, та остаётся на месте.

## 289
- Дальность, выставленная хостом, теперь применяется сразу на всех сборках. Раньше так было только на старых версиях, а на остальных ждала до пяти секунд.

## 288
- Внутренняя правка без видимых отличий.

## 287
- В отчёт теперь попадает, какие игровые улучшения удалось поставить на сборку. Раньше по логу нельзя было отличить отсутствующую возможность от сломанной.

## 286
- Гость больше не остаётся с кольцом дыр в мире: чанки уходили ему раньше разрешения их принять, и второй раз сервер их уже не слал. Ползунок хоста применяется сразу.

## 285
- Раздача дальней прорисовки больше не съедает сервер: на каждый отправленный чанк лаунчер заново искал имена внутри игры. Теперь он находит их один раз.

## 284
- Внутренняя правка в независимой прорисовке, без видимых отличий.

## 283
- На All The Fabric 3 и RPG Ars Nouveau мир рисовался на кольцо ближе выбранного: игра хранит это число иначе, и теперь лаунчер это учитывает. Частые движения ползунка больше не подвешивают игру хосту.

## 282
- Каждый гость видит на ту дальность, которую выставил у себя, а не на общую для всех. Ползунок в игре применяется сразу, без перезахода.

## 281
- Гости наконец видят дальше хоста: мир раздавался дальше, но клиенту об этом не говорили, и он выбрасывал всё лишнее. Теперь новая дальность объявляется.

## 280
- В All The Fabric 3 вернулись скин и постоянный UUID: проверка адаптера требовала строчку чата, которой в 1.18.2 нет, и из-за неё не ставился весь адаптер.

## 279
- Сборки в списке идут по алфавиту, скачанные и предложенные вместе, и без выбора берётся первая скачанная. Игроки в обоих списках теперь в одном порядке.

## 278
- All The Fabric 3 тоже открывает мир для сети по одной кнопке, как и остальные сборки. Для этого лаунчер докачивает недостающие имена игры один раз на сборку.

## 277
- Сборки, чьи имена лаунчер прочитать не может, снова получают скин и свой UUID: с 30 августа они запускались вообще без этого. RPG Ars Nouveau вдобавок открывает мир по одной кнопке.

## 276
- Лаунчер научился читать имена, которыми Forge называет части игры. Сам по себе этот шаг ничего не меняет: он нужен, чтобы хуки заработали и на Forge-сборках.

## 275
- Папка с отложенными при обновлении файлами больше не копит одно и то же: одинаковая копия хранится один раз. Снимков на сборку остаётся пять последних.

## 274
- Моды больше не пишут в профиль Windows: папка сборки теперь отвечает и на %APPDATA%, и на %USERPROFILE%. Всё, что мод сохранял вне установки, лежит внутри неё.

## 273
- Мир, удалённый в игре, больше не оставляет за собой пустую папку. Такие папки убираются и у тех, у кого они уже накопились.

## 272
- Мир раздаётся ровно настолько, насколько просит самый дальнозоркий из присутствующих. В одиночку хост грузит своё число, а не чужое.

## 271
- Мир, открытый для сети, раздаётся на 32 чанка независимо от того, сколько рисует хост. Каждый гость видит на свою дальность, а не на хозяйскую.

## 270
- Хост может раздавать больше чанков, чем рисует сам: гости выбирают свою дальность до этого потолка. Пока задаётся числом в настройках, окно будет позже.

## 269
- Уборка инстанса больше не удаляет пустые папки, созданные модами: из-за этого KubeJS писал красную ошибку при каждом запуске. Уже удалённые папки возвращаются.

## 268
- Обводка, остающаяся на кнопке после нажатия, теперь плавно гаснет за треть секунды. Появляется она по-прежнему сразу, чтобы нажатие было видно.

## 267
- Мод, полезший в интернет из игрового такта, больше не морозит мир на двадцать секунд. Соединение, которое не установилось за пять секунд, теперь просто обрывается.

## 266
- Сборки теперь выкладываются обновлением на месте, а не сносом и повторной выкладкой. Неудачная публикация оставляет прежний пак на месте, а не без пака вовсе.

## 265
- Поле состояния больше не пустует: пока ничего не случилось, в нём стоит «Всё работает :)». Если Steam не подключён, оно говорит включить его и нажать «Повторить».

## 264
- Скин из лаунчера снова виден на моделях Steve и Alex из Yes Steve Model. Игра больше не пересчитывает лицо игрока при каждом обращении и берёт готовое, а не запасное.

## 263
- Открытие мира для сети больше не роняет игру: экран настроек успевает построиться, и мод, который его дополняет, находит свои кнопки на месте. Мир открывается с одного нажатия, как и раньше.

## 262
- У поля состояния появилась подпись «Статус» слева, как у «Описания» над ним.

## 261
- Строка состояния под описанием стала полем: тем же на вид, что и поле описания, но писать в него нельзя, а читать, листать и выделять можно. Длинное сообщение больше не обрывается на полуслове.

## 260
- Повторное нажатие «Открыть для сети» на уже открытом мире просто закрывает меню, а не показывает экран настроек, менять которые уже нечем. Убрана правка скинов, которая ничего не чинила.

## 259
- Кнопка пресета больше не дышит, пока считается оценка памяти: у оценки теперь своё место, и бегущее многоточие её не двигает.

## 258
- В оценке памяти теперь написано «от», а не знак «примерно». И сборка снова собирается: сторож второй копии мог уронить процесс, когда его отпускали.

## 257
- Выбор сборки больше не подвешивает окно: пока лаунчер взвешивает сборку, на месте оценки памяти бежит многоточие, а число появляется, когда счёт готов.

## 256
- Номер версии в окне и номер в списке «Что нового» снова совпадают. Кнопка пресета стала шире, чтобы отступ до оценки памяти держался ею, а не сдвигом надписи.

## 255
- Переносимые скины и личность вернулись: проверка перед запуском не знала о двух новых правках и на всякий случай отключала их все. Скин из лаунчера снова виден.

## 254
- Второй лаунчер больше не запускается: повторный клик по значку выводит вперёд уже открытое окно. Отступ у оценки памяти стал таким же, как у шкалы передачи мира.

## 253
- Статус Steam встал в подвал слева от кнопки «Новости», а кнопка «Повторить» рядом с ней. Поле для сообщения больше никуда не съезжает.

## 252
- «Открыть мир для сети» снова открывает мир сразу: без экрана настроек, на свободном порту, с игровым режимом самого мира. Кнопка после этого становится «Жалобы», как в игре, а зайти могут все друзья в Steam.

## 251
- В репозитории появился MESSAGES.md: все надписи, которые лаунчер может показать, и когда каждая появляется. Для игры ничего не меняется, это справочник.

## 250
- Подвал стал одной строкой одного размера: слева надпись, справа переключатель, на одном уровне. Steam переехал под поле чата, и кнопка «Повторить» теперь такая же, как остальные, и кончается там же, где поле.

## 249
- Сборка больше не может сломаться из-за невидимой метки в начале файла: теперь это ловит тест, а не падение сборки релиза. Для игры ничего не меняется.

## 248
- Сборка лаунчера снова собирается: в двух файлах адаптера появилась невидимая метка в начале файла, из-за которой компилятор Java отказывался их читать. На поведение игры это не влияло никак.

## 247
- Скин из лаунчера снова виден на моделях Steve и Alex в Yes Steve Model, и настраивать в самом моде ничего не нужно. Игра больше не отдаёт моду базовое лицо, пока скин ещё едет с этой же машины.

## 246
- Кнопка пресета управления сузилась до своей надписи, а слева от неё встала оценка памяти для выбранной сборки. Число в поле RAM остаётся вашим: оценка только говорит, сколько лаунчер советует этой сборке.

## 245
- Сборка теперь может закрепить за собой настройку игры, и при каждом запуске лаунчер ставит её обратно: в LL8 так закреплена прозрачность фона текста на 0%. Остальные настройки сборки остаются начальными и ваш выбор не трогают.

## 244
- В поле RAM теперь можно поставить всё, что есть в компьютере, кроме доли для системы: на 32 ГБ это 28. Потолок больше не зависит от сборки, видеокарты и прошлых сессий, поэтому число не меняется само между запусками.

## 243
- Число в поле RAM теперь и есть куча Java: сколько поставили, столько и покажет игра. Запас на моды и буферы вычитается из потолка поля, а не из вашего числа, поэтому число стало меньше, а памяти игре достаётся столько же.

## 242
- Подложка под ником снова рисуется как в игре: править её со стороны лаунчера было шире самой беды, потому что чёрный прямоугольник делает шейдерпак, а не игра. Лечим в сборке.

## 241
- Подложка под ником убирается только там, где включены шейдеры, и вместо неё у букв появляется тень. На сборках без шейдеров ник остаётся ровно таким, каким был.

## 240
- Ник над игроком больше не рисуется на чёрной подложке, когда включены шейдеры: у многих паков она превращалась в сплошной прямоугольник поверх имени. Вместо неё у букв появилась тень.

## 239
- Запас памяти рядом с кучей больше не растёт на десятую сверх того, что игра когда-либо брала: запас нужен полу, а не потолку. На машине с картой на 8 ГБ это возвращает сборке гигабайт кучи.

## 238
- Замер прошлых сессий больше не решает за оценку, а лишь держит её в границах: видеокарта резервирует свой объём в системной памяти, и из-за этого сборке доставалась вдвое меньшая куча. Куча снова прежняя.

## 237
- Внутренние изменения без видимых отличий.

## 236
- Скины других игроков видны и на старых сборках: игра требовала подпись Mojang, которой у скина из лаунчера быть не может, и показывала всем только их собственный скин. Теперь это требование снимается для скинов лаунчера.

## 235
- Скины снова видны сразу после обновления: раздача скинов бралась за один и тот же порт, и когда его держала прошлая копия лаунчера, все ходили в стандартном скине. Теперь берётся любой свободный порт.

## 234
- Новый ник принимается только латиницей, цифрами и подчёркиванием: команды игры читают имя латиницей, и к игроку с русским ником нельзя было телепортироваться. Уже сохранённые ники остаются как есть.

## 233
- Гость на чужом мире снова играет своим персонажем: туннель Steam подменял ему личность, и лаунчер теперь узнаёт эту подмену. Куча считается по фактическому расходу прошлых сессий, а не по оценке.

## 232
- Внутренние изменения: из репозитория убраны рабочие записи о переносе мира на LL8. Для игрока ничего не меняется.

## 231
- Занятая сеть больше не мешает запуску: файл e4steam лаунчер докачивает и повторяет попытку, а не бросает на первой заминке. Медленная загрузка теперь считается загрузкой, а не обрывом.

## 230
- All The Fabric 3 больше не падает при запуске: Fabric отказывался отдавать игре служебный класс лаунчера, потому что видел его сразу в двух местах. Теперь он там один.

## 229
- All The Fabric 3 снова доходит до игры: лаунчер подсовывал загрузчику свою копию служебной библиотеки, и Fabric переставал находить свою. Проверка личности заодно перестала ломаться на будущих версиях Minecraft.

## 228
- Созданный мир переезжает к остальным сразу, как только вы из него вышли, а не когда закроете игру. До этого он часами лежал внутри сборки и не был виден ни в списке, ни для передачи.

## 227
- Выпуск снова публикуется: в списке изменений не хватало записи, и сборка на сервере останавливалась на этом. Для игрока ничего не меняется.

## 226
- All The Fabric 3 снова запускается: окно с ошибкой про версию Java больше не появляется. И ни одна сборка теперь не откажется стартовать из-за того, что лаунчеру не подошли её внутренности.

## 225
- Внутренние изменения без видимых отличий.

## 224
- Ваши вещи в мирах друзей теперь привязаны к аккаунту Steam на любой версии игры и любом загрузчике. Сменили компьютер, вошли через Steam, докачали сборку: инвентарь на месте.

## 223
- Панель справа теперь называется «AI-помощник». Кнопка под ней стала того же размера, что и «Отправить» рядом с полем ввода.

## 222
- Скины заработали на All The Fabric 3 и RPG Ars Nouveau, а заодно на любой будущей сборке. Раньше лаунчер выключал их вместе с крюками, которые этим версиям не подошли.

## 221
- Игра больше не предлагает имя мира, который уже занят в другой сборке: она сама назовёт новый «New World (1)». А скины заработали на сборках до Minecraft 1.19.4, где их не было видно вообще.

## 220
- Предупреждение о пропавших модах молчит, когда мод просто обновили до новой версии. Зато теперь замечает и скрипты сборки, которые добавляли в мир блоки и предметы.

## 219
- Если обновление сборки убирает мод, лаунчер скажет об этом до запуска: всё, что этот мод поставил в мире, пропадёт при открытии.

## 218
- Два мира с одинаковым именем из разных сборок больше не прячут друг друга: второй переезжает под своим именем папки, а в игре называется как назвали.

## 217
- Память под сборку считается по модам, которые игра действительно грузит, а не по файлам: обещанного числа теперь хватает.

## 216
- Миры снова появляются в списке передачи: пустая папка от прошлого запуска больше не выдаёт себя за мир и не загораживает настоящий.

## 215
- Моды с не совсем правильным JSON снова читаются: сборка на них определяется, а не отбрасывается с ошибкой.

## 214
- Лаунчер больше не навязывает сборкам чужие настройки: теперь сборка сама говорит, с какими опциями Java её запускать.

## 213
- Сборки на старых версиях с Continuity снова запускаются: лаунчер больше не навязывает им настройку ModernFix, с которой они несовместимы.

## 212
- Своя сборка готова из одной папки с модами: лаунчер сам узнаёт версию, загрузчик и всё докачивает. Клиент игры паку больше не нужен.

## 211
- Длинное сообщение внизу окна больше не срезает футер. Память перестала прилипать к сборке от одного клика по полю, и лаунчер предупреждает, когда её мало.

## 210
- Сборки на Fabric и Quilt снова находят игру и запускаются. Уже готовые чинятся на месте, без повторной загрузки.

## 209
- Сборки на Fabric и на старых версиях больше не отказываются запускаться. Выбранная память теперь запоминается для каждой сборки отдельно.

## 208
- Играть вместе теперь можно на любой версии Minecraft от 1.7 до 26.2 и на любом из четырёх загрузчиков, а не только на новых.

## 207
- Сборка снова проходит проверку: два теста памяти держали число, которое зависит от объёма ОЗУ проверяющей машины. Для игрока ничего не меняется.

## 206
- Каждая сборка запускается на той Java, под которую сделана, и Java теперь одна на все сборки, а не своя копия у каждой. Лёгкие сборки приезжают с уже выставленной под слабый ноутбук графикой.

## 205
- В списке появилась RPG Ars Nouveau: 73 мода на 1.20.1, единственная из наших сборок, которой хватает трёх гигабайт кучи на ноутбуке с восемью. Рендерер и настройки графики в ней уже выставлены под слабую машину.

## 204
- Сборки в списке названы полностью: All The Mods 10 и All The Fabric 3 вместо ATM10 и E10. Миры, созданные под старым именем, в переименованной сборке больше не показываются.

## 203
- Java обновилась до 21.0.12.1: при первом запуске она скачается заново, около 200 МБ, и дальше всё как обычно.

## 202
- На тесной машине игра больше не занимает всю кучу в первый же миг, а дорастает до неё, и сборщику мусора там даны свои настройки. На просторной всё как было: куча сразу целиком, без рывков.

## 201
- Играть вместе теперь можно и на сборках Forge, Fabric и Quilt, а не только на NeoForge: у мода связи есть сборка под каждый загрузчик. Сам мод обновлён до 0.3.0.

## 200
- Запись о памяти после игры теперь называет, сколько сборка запросила, а не только сколько успела занять. На тесной машине второе занижало, потому что лишнее уходило в подкачку.

## 199
- Память, вписанную в файл настроек мимо окна, лаунчер больше не берёт на веру: она режется так же, как в самом окне. После каждой игры он записывает, сколько она держала на самом деле.

## 198
- Машине, которой сборка не по силам, лаунчер больше не советует число, которое она же и не даст ввести. Вместо него короткая строчка про слабую ауру.

## 197
- Подпись у поля памяти теперь «ГБ», а не «GB». Рекомендуемая память считается по объёму машины, а не выбирается из нескольких заготовленных чисел.

## 196
- Предупреждение о нехватке памяти стало в одну строку: сколько мало и сколько поставить в RAM.

## 195
- Предупреждение о нехватке памяти теперь называет число, с которым сборка будет играться, а не то, ниже которого куче уже некуда падать.

## 194
- Лаунчер больше не отнимает память у сборки из-за встроенной видеокарты: он принимал служебное число драйвера за 2 ГБ видеопамяти. И теперь предупреждает заранее, если выставленной памяти сборке заведомо мало.

## 193
- В списке сборок появилась Enigmatica 10, она же E10. Её видно и можно запустить, даже если папки с ней у вас ещё нет.

## 192
- Эмодзи в имени мира, в переименованном предмете или в книге больше не превращаются в «ромбики»: лаунчер пишет текст так же, как игра. Машинам до 16 ГБ он теперь предлагает на гигабайт памяти больше.

## 191
- Лаунчер смотрит на видеокарту: если сборке не хватает видеопамяти, часть текстур драйвер держит в оперативной, и куче Java достаётся меньше. На карте с 8 ГБ тяжёлой сборке теперь считается 12 ГБ кучи вместо 16.

## 190
- Кнопка пресета управления на сборке без своей раскладки просто не нажимается и молчит: подсказки про отсутствующий пресет больше нет. Остальные её состояния объясняются как прежде.

## 189
- Отказ от приёма мира больше не оставляет полосу «Ожидание подтверждения» висеть до перезапуска. Оценка времени передачи считается от её начала, а не от ожидания ответа, и больше не завышена вначале.

## 188
- В списке сборок появилась The Broken Script Enhanced. Её видно и можно запустить, даже если папки с ней у вас ещё нет.

## 187
- Ресурспак с амперсандом в имени снова встаёт на своё место в списке. Игра записывала его имя экранированным, и лаунчер не узнавал собственный пак.

## 186
- Сборка снова проходит проверку: уборка временных папок в тестах спорила с Windows, которая отпускает файл мгновением позже. Для игрока ничего не меняется.

## 185
- В лаунчере видны все миры, какой бы сборке они ни принадлежали: чужой мир теперь можно передать, не переключая сборку. В самой игре по-прежнему только свои миры.

## 184
- Если сборка передумала насчёт ресурспака, её решение доходит до всех: у жителей теперь по умолчанию Luigi, а THICC лежит выключенным. Дальше выбор снова ваш и сохраняется.

## 183
- Порядок ресурспаков держит лаунчер: каждый запуск возвращает паки сборки на свои места и включает их обратно. Исключение одно: жителей можно переключить с Luigi на THICC, и этот выбор сохраняется.

## 182
- Отчёт об ошибке теперь перечисляет миры и сборку, к которой приписан каждый. Строка запуска Java в логе снова читается, а не показывает имя типа шестьдесят раз подряд.

## 181
- Сборка снова проходит проверку: тест ширины полосы передачи ждал русского формата чисел и падал на английской системе. Для игрока ничего не меняется.

## 180
- Отчёт об ошибке теперь называет строку запуска Java и перестал перечислять каждый мод дважды. Стало видно, с какими настройками памяти и флагами запускалась игра.

## 179
- Полоса передачи мира теперь показывает время до конца всей передачи, а не текущего шага. Оценка строится по тому, как долго шли прошлые передачи, и уточняется по ходу.

## 178
- На полосе передачи мира теперь видно, сколько осталось ждать. Оценка считает текущий этап: копирование, сжатие, отправку или распаковку.

## 177
- Мир больше не достаётся той сборке, которая первой открыла список: принадлежность решает только игра на нём. Поэтому мир из одной сборки перестанет показываться в другой.

## 176
- Исправлен запуск: в прошлой версии игра не стартовала с ошибкой «Could not create the Java Virtual Machine». Виноват был один лишний параметр памяти, он убран.

## 175
- Память игре теперь выделяется сразу целиком, а сборщик мусора настроен на короткие паузы: меньше подёргиваний в игре. Из журналов убран шум, из-за которого отчёты приходили неполными.

## 174
- Правки настроек из сборки теперь доходят и до тех, у кого игра уже создана: сборка может объявить файлы своими, и они перезаписываются. Прежняя копия сохраняется, её можно вернуть.

## 173
- Мир, у которого не записана сборка, теперь получает её после того, как на нём поиграли, и дальше виден только там. Так мир из одной сборки перестаёт предлагаться в другой.

## 172
- Из репозитория лаунчера убраны два файла модов, попавшие туда по ошибке. На игру и на сборки это никак не влияет.

## 171
- Игрок, закрывший лаунчер, сразу пропадает из списка у остальных, и отправить ему отчёт больше не предлагается. Сам отчёт стал полнее: в него попадают начало игрового лога, консоль игры и файлы сбоя Java.

## 170
- Не установленная сборка помечена звёздочкой после названия, как ATM10*. Так сразу видно, что её ещё предстоит скачать.

## 169
- Сборки, которые лежат в гите, теперь видны в списке у всех, даже если папки с ними ещё нет. Нажатие «Играть» такую сборку скачивает.

## 168
- Мир показывается только в той сборке, где он создан: мир с ATM10 больше не предлагается в LL8 Extended и наоборот. Миры без записанной сборки и миры прежних имён сборки видны по-прежнему.

## 167
- Путевые точки снова обновляются перед передачей мира: миры со старым именем сборки лаунчер теперь переименовывает по-настоящему. Мир, пришедший от друга на старой версии, тоже понимается правильно.

## 166
- Yes Steve Model снова показывает модели: лаунчер больше не переписывает выбор игрока и не правит настройки мода, а прежний выбор вернули из его же резервных копий. Мод стартует со своими настройками, как при первой установке.

## 165
- Надпись «YSM Model Sync State: Loading» больше не висит в углу у хозяина мира: лаунчер один раз выключает этот баннер в настройках мода. Сами модели он не чинит, но и молчать о загрузке, которой нет, больше не будет.

## 164
- Сборка теперь называется «LL8 Extended»: лаунчер переименует свои папки сам, миры и прогресс на месте, качать заново ничего не нужно. На кнопке «Играть» последний шаг подготовки теперь говорит «Готовится к запуску».

## 163
- Кнопка «Обновить» больше не ждёт Steam: новую версию лаунчера можно поставить и без него. Остальные действия по-прежнему включаются только тогда, когда Steam назвал игрока.

## 162
- В свойствах файла LANMinecraft.exe версия продукта теперь совпадает с номером версии в лаунчере, а не показывает всегда 1.0.0.

## 161
- Технический выпуск: лаунчер пересобран из обновлённой истории проекта. В работе программы ничего не изменилось.

## 160
- Отчёт о переносе мира со старой сборки теперь хранится рядом с лаунчером: в нём записано, что именно исчезло из мира и у кого. На саму игру это не влияет.

## 159
- Правая колонка теперь переключается кнопкой в углу: лаунчер открывается на чате с AI-помощником, а «Новости» показывают историю версий. Номер версии переехал в строку «Вы на последней версии».

## 158
- Кнопка пресета управления больше не загорается после каждого выхода из игры: строки раскладки для клавиш, которых ни один мод сборки не заводит, перестали считаться несовпадением. Их список уходит в лог.

## 157
- Лаунчер считает память по самой сборке: сколько в ней модов и текстур, столько он и держит вне кучи Java. Лёгкой сборке предложит меньше гигабайт, тяжёлой больше, а введённое вручную число не трогает.

## 156
- Запас под родную часть игры больше не растёт вместе с бюджетом, а упирается в 8 ГБ: классы девятисот модов и буферы Sodium занимают своё независимо от кучи. Прежние 12 ГБ кучи теперь означают бюджет 20 ГБ.

## 155
- Число в поле памяти теперь вся память игры, а не одна куча Java; прежняя настройка переносится без потери. Игра закрывается на первой нехватке памяти, не успевая предложить открыть мир без модов.

## 154
- Кнопка пресета управления теперь говорит, что именно нашла: в подсказке стоит первая строка раскладки, которой в игре нет. Описания версий стали короче - два предложения на версию.

## 153
- Возврат обычной модели в Yes Steve Model теперь и правда возврат: у мода для этого отдельный выключатель, а не имя модели. Выбранная модель остаётся на месте, если включить мод обратно.

## 152
- «Сообщить о проблеме» больше не блокируется и пишет причину словами под кнопкой. Ресурспаки перестали пропадать при запуске: снята пометка «устаревший» и убраны пять паков, которые и так включает Paxi.

## 151
- Кнопка пресета управления оживает сразу, а не после запуска игры: лаунчер сам подтягивает раскладку сборки и сверяет её с настройками. Сборка, выбранная автоматически, тоже запоминается.

## 150
- Набор ресурспаков у игрока теперь ровно тот, что в сборке: лишние снимаются с выбора, свои паки игрока не трогаются. Сборка может попросить вернуть всем обычную модель в Yes Steve Model.

## 149
- Сборка может попросить сбросить нарисованную карту, и лаунчер делает это один раз у каждого игрока. Стираются только плитки, точки и настройки остаются.

## 148
- Передача мира больше не ползёт: ограничитель скорости падал к 256 КБ/с и не поднимался обратно. Теперь его пол - 2 МБ/с, а темп ищет сам Steam.

## 147
- Отчёт об ошибке больше не портит вчерашние журналы: сжатые `.log.gz` он читал как текст. Теперь они распаковываются, и размер отчёта от этого не растёт.

## 146
- Подписи на кнопке «Играть» стали короткими и влезают целиком. Шкала «В ожидании мира» гаснет вместе с кнопкой «Передать», когда передавать нечего или некому.

## 145
- Ресурспаки сборки встают в заданном ею порядке, а не поверх всего. Порядок задаётся один раз на версию списка, поэтому перестановки игрока переживают перезапуск.

## 144
- Игра переехала на Java 21 вместо Java 25, и Cobblemon вернулся в сборку: мод запрещает себе всё новее 21. Лаунчер ставит её сам, а прошлую убирает с диска.

## 143
- Сборка 142 не вышла: в неё уехал код, который не собирается. Здесь он собран, и всё обещанное в 142 работает.

## 142
- Повторяющиеся строки в настройках управления убираются перед каждым запуском, а не только по кнопке. Ресурспаки сборки включаются один раз сами, история версий грузится по мере прокрутки.

## 141
- Игра снова запускается: пресет управления оставлял в настройках игры две одинаковые строки, а на такой строке NeoForge падает ещё до окна загрузки. Теперь применение пресета убирает любые повторы, чьи бы они ни были.

## 140
- Если игра закрывается сама уже после запуска, лаунчер пишет в журнал код выхода, то, что игра успела сказать в поток ошибок, и хвост её собственного лога. Раньше о таком выходе не оставалось ни строчки.

## 139
- Лаунчер снова открывается: в 139 исправлен запуск, сломанный в 138, где окно при создании просило журнал, которого на тот момент ещё нет.

## 138
- Ход имени больше не пропадает: одна перекладка окна останавливала его навсегда. Лаунчер записывает в журнал, сколько имени спрятано.

## 137
- Ход длинного имени стал заметным: раньше имя, у которого прятался один символ, сдвигалось за доли секунды, и движение было не разглядеть.

## 136
- Имя, которое не помещается в поле, снова едет: лаунчер считал ширину сам и решал, что имя влезает, хотя игрок видел его обрезанным.

## 135
- Длинное имя, которое не помещается в поле, само едет к своему концу и обратно, пока вы его не редактируете; короткое имя стоит на месте.

## 134
- Значение оперативной памяти стоит по центру своего поля.

## 133
- Новое оформление «Обсидиан»: тёмное окно вместе с заголовком, шрифт Montserrat, изумрудная кнопка «Играть». Функции и расположение элементов те же.

## 132
- Подготовка к новому оформлению: в лаунчер вшит шрифт Montserrat, а все цвета, размеры и отступы интерфейса собраны в одном месте как именованные значения; на экране пока ничего не изменилось.

## 131
- Окно больше не срезает футер и шкалу обновления: полотну не хватало 25 пикселей. Настройки под «Играть» разложены на две равные половины.

## 130
- Рядом с выбором сборки появилась кнопка «Пресет настроек управления»: она записывает в игру раскладку без конфликтов. Кнопка гаснет, когда пресет применён, и снова предлагает себя после изменений.

## 129
- Обновление доходит до игроков вдвое быстрее: сборка не скачивает лишнее, а тесты и патчи считаются параллельно. В лаунчере ничего не изменилось.

## 128
- Публикация релиза переживает перебои GitHub: каждый запрос к нему повторяется с нарастающей паузой, пока сервер отвечает ошибкой 5xx, а не ответом; в лаунчере ничего не изменилось.

## 127
- Подготовка сборки показывается прямо в кнопке «Играть», у запущенной игры она так и говорит. Кнопка отчёта стала «Отправить» и встала рядом со списком.

## 126
- «Сообщить о проблеме» переехало в основную часть окна, между передачей мира и шкалами загрузки; справа появилось «Что нового» - история версий, а Steam ушёл в футер: ник или кнопка «Повторить».

## 125
- Публикация релизов устойчивее к сбоям GitHub; в лаунчере ничего не изменилось.

## 124
- Оверлей Steam (Shift+Tab) снова работает в игре.

## 123
- Вернулась ленивая загрузка моделей; для машин с 16 ГБ памяти лаунчер предлагает 12 ГБ.

## 122
- Скорость передачи мира подбирается плавнее, без рывков; друзья представляются друг другу сами, даже если Steam не отдал присутствие.

## 121
- Мир передаётся на той скорости, которую держит канал, и она честно показана.

## 120
- Получатель отчёта выбирается так же, как получатель мира: пусто - список заблокирован, появился в сети - выбран.

## 119
- Лаунчер больше не вмешивается в загрузку ресурсов сборки.

## 118
- Скин можно менять до самого старта клиента; отчёт можно отправить любому, кто в сети.

## 117
- Файлы сохраняются, даже если их в этот момент кто-то читает.

## 116
- Ник и память блокируются только когда игра их уже забрала; мир и игрок для передачи - пока игра запущена.

## 115
- В версии показывается один номер, без хеша коммита.

## 114
- Номер версии равен номеру коммита.

## 113
- Сбой записи файла сборки больше не выглядит как обрыв связи; замена файлов повторяется, если их держит антивирус.

## 112
- Сборка LL8: чистая TNP Limitless 8 вместо слияния с Infinity; папки Infinity переименовываются автоматически.

## 111
- Один медленный получатель больше не тормозит остальные соединения.

## 110
- Точнее таймер отправки; передача быстрее.

## 109
- Запросы к Steam о друзьях реже; в логе видно, что он ответил.

## 108
- Друг с непрочитанным присутствием больше не считается «старой версией».

## 107
- Скорость сети измеряется, а не оценивается.

## 106
- Список сборок из одного пункта заблокирован, как и список игроков.

## 105
- Список из одного пункта не притворяется выбором.

## 104
- Игрок, с которым уже есть связь, остаётся в списке независимо от Steam.

## 103
- Пустой список игроков объясняет, почему он пуст.

## 102
- Лаунчер замечает, когда Steam отклоняет настройку соединения.

## 101
- Модели загружаются по запросу - игра стартует и меняет ресурспаки быстрее.

## 100
- Репозиторий назван LANMinecraft, как и программа.

## 99
- Принимающая сторона ждёт дольше отправляющей; соединения рвутся реже.

## 98
- Приём мира большими блоками - быстрее.

## 97
- Показывается реальная скорость передачи, а не скорость заполнения буфера.

## 96
- Точнее снимок окружения в отчёте; быстрее передача.

## 95
- Внутренние изменения без видимых отличий.

## 94
- Друзья не пропадают из списка при паузах Steam; отчёт показывает, что он делает.

## 93
- В списке видно, у кого из друзей запущен лаунчер.

## 92
- Удалены остатки кода до перехода на Steam.

## 91
- Удалён код для лаунчеров, которых больше нет.

## 90
- Исправления по итогам проверки кода.

## 89
- Несовпадение версий показывается понятным сообщением.

## 88
- В отчёт попадает до 10 МБ каждого лога.

## 87
- Многопользовательская игра полностью переведена на Steam.

## 86
- Единая версия форматов файлов и сетевого протокола.

## 85
- Исправлены ошибки, найденные при проверке кода.

## 84
- В отчёт об ошибке вложен снимок окружения.

## 83
- Вместо потока логов - отчёт об ошибке по кнопке.

## 82
- Логи занимают меньше места на диске и в сети.

## 81
- Внутренние изменения без видимых отличий.

## 80
- Переписано описание проекта.

## 79
- Удаляются файлы, оставшиеся от старых версий лаунчера.

## 78
- Карта Xaero находит свою папку, даже когда адрес сервера меняется.

## 77
- Удалены миграции с версий до Steam.

## 76
- Игроки привязаны по SteamID.

## 75
- В файлах мира записываются Steam-аккаунты игроков.

## 74
- Восстановление профилей после вылета действительно работает.

## 73
- Исправлены две ошибки в передаче данных через Steam.

## 72
- Перед приёмом мира у игрока спрашивают согласие.

## 71
- Исправлена остановка передачи логов вскоре после запуска игры.

## 70
- Данные между игроками идут через Steam.

## 69
- Игроки - это друзья Steam; IP-адреса больше не нужны.

## 68
- Друзья в Steam видят, что вы в лаунчере.

## 67
- Лаунчер запускается через Steam и привязанный профиль.

## 66
- Steam-аккаунт привязывается к профилю с вашим прогрессом.

## 65
- Внутренние изменения без видимых отличий.

## 64
- Удалена игра по сети через VPN.

## 63
- Голосовой чат удалён.

## 62
- Диагностика перенесена в главное окно; окно голоса убрано.

## 61
- Файл настроек получил версию; старая копия сохраняется.

## 60
- Подготовка к привязке профилей к Steam-аккаунту.

## 59
- Закреплён мод e4steam: любая Steam-сборка может создавать и принимать игру.

## 58
- Лаунчер подключается к Steam.

## 57
- Внутренние изменения без видимых отличий.

## 56
- Правила телепортов закреплены за NeoForge 21.1.244.

## 55
- Сохранение мира повторяется, если файл занят антивирусом.

## 54
- Лаунчер переименован в LANMinecraft.exe.

## 53
- Сборка Infinity обновляется из репозитория при нажатии «Играть».

## 52
- В строгом выживании телепорты ограничены; игра занимает меньше памяти.

## 51
- Игра запускается на Java 25, которую лаунчер ставит сам.

## 50
- Внутренние изменения без видимых отличий.

## 49
- Внутренние изменения без видимых отличий.

## 48
- В выживании можно телепортироваться к точкам на карте без читов.

## 47
- Мир открывается для сети сразу, без экрана настроек.

## 46
- Исправлен вылет из-за кирки Promethium.

## 45
- Исправлен вылет в измерении Bumblezone.

## 44
- Временные файлы передачи удаляются в фоне.

## 43
- Закладки JEI сохраняются между запусками.

## 42
- Передача мира надёжнее переживает зависания и убирает временные файлы.

## 41
- Внутренние изменения без видимых отличий.

## 40
- Миры любого размера передаются на полной скорости сети.

## 39
- Исправлено резервное скачивание компонентов игры.

## 38
- Добавлены команды телепорта в выживании.

## 37
- Сессии в локальной сети восстанавливаются после обрыва ретранслятора.

## 36
- Внутренние изменения без видимых отличий.

## 35
- Можно отправить свои логи другому игроку для разбора проблемы.

## 34
- Внутренние изменения без видимых отличий.

## 33
- Внутренние изменения без видимых отличий.

## 32
- Внутренние изменения без видимых отличий.

## 31
- Для игры по сети используется выбранный IP-адрес.

## 30
- Упрощены окно лаунчера и настройка сети.

## 29
- Стабилизированы соединения с игроками и сессии в локальной сети.

## 28
- Снято ограничение на число известных игроков.

## 27
- Исправлены поиск игроков через VPN, маршруты в локальной сети и восстановление голоса.

## 26
- Выпуск под старым номером 17; видимых изменений нет.

## 25
- Внутренние изменения без видимых отличий.

## 24
- Упрощена установка Hamachi.

## 23
- Установщик Hamachi скачивается с GitHub с проверкой подлинности.

## 22
- Исправлены поиск игроков через ретранслятор и восстановление голоса.

## 21
- Уточнены настройки голоса и сети.

## 20
- Поправлен значок выключенного буфера голоса.

## 19
- Скачанное обновление ставится при выходе из лаунчера.

## 18
- Исправлена публикация обновлений; в лаунчере ничего не изменилось.

## 17
- Исправлены защита голосового чата и доставка обновлений.

## 16
- Появился значок выключенного буфера голоса.

## 15
- Исправлены скины игроков.

## 14
- Проверка обновлений не ломается без интернета.

## 13
- Сетевая игра и лаунчер стали надёжнее.

## 12
- Игра по сети через любое подключение; у версий появились номера.

## 11
- Голосовой чат устойчивее к плохой сети.

## 10
- Улучшены голосовой чат и передача скинов.

## 9
- Выровнены кнопки и полосы прогресса.

## 8
- Исправлена сборка релизов; в лаунчере ничего не изменилось.

## 7
- Доработаны окно лаунчера и механизм обновления.

## 6
- Исправлены голосовой чат по локальной сети и передача профилей игроков.

## 5
- Обновления скачиваются как разница с прошлой версией, а не целиком.

## 4
- Внизу окна появилась подпись «Лучше TLauncher».

## 3
- Лаунчер больше не хранит запасные копии модов.

## 2
- Внутренние изменения без видимых отличий.

## 1
- Первый публичный выпуск лаунчера.
